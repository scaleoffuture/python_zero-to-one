# Tuples are like lists, but which you can't change (adding/shriking/inserting/deleting are't allowed).
# You should mark them with just parentheses instead of square brackets.

dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])
dimensions[0] = 250
